{% extends 'base.html.twig' %}

{% block title %}Create Garage{% endblock %}

{% block body %}
    <h1>Create New Garage</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-3">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('garage_index') }}" class="btn btn-link mt-2">Back to list</a>
{% endblock %}
