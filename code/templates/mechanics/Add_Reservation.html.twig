{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Ajouter une réservation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}
