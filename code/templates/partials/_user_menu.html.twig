<div class="wptb-header--cart">
    <a href="#" class="wptb-cart-icon wptb-person-icon"><i class="bi bi-person"></i></a>
    <div class="wptb-cart-box wptb-person-box">
        <div class="wptb-grand-total">
            <ul>
                {% if isLoggedIn %}
                    <li><a href="{{ path('app_logout') }}">Logout</a></li>
                {% else %}
                    <li><a href="{{ path('app_login') }}">Login</a></li>
                {% endif %}
                <li><a href="shop-product.html">My commands</a> <span class="value"></span></li>
                <li><a href="{{ path('app_history_client') }}">My reservations</a> <span class="value"></span></li>

            </ul>

        </div>
    </div>
</div>